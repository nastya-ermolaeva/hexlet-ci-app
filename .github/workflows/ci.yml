name: ci-workflow
on: push
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: установка зависимостей
        run: make setup

      - name: запуск линтера
        run: make lint

      - name: запуск тестов
        run: make test
